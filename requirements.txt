# 核心依赖
ollama>=0.4.0
psutil>=5.9.0
pyyaml>=6.0

# 云端API支持 (可选)
openai>=1.0.0

# 测试依赖 (必须)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# 开发依赖 (可选)
black>=23.0.0
ruff>=0.1.0

# 性能监控 (可选)
nvidia-ml-py>=12.0.0; platform_system == "Windows" or platform_system == "Linux"
